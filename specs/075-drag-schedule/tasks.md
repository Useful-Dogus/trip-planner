# 075-drag-schedule — Tasks

- [ ] T1: `DateGroupHeader` 에 `useDroppable` 통합 + 활성 시각 표시
- [ ] T2: "날짜 미정" 헤더 (ScheduleTable 안의 두 곳) 에 동일 droppable 적용
- [ ] T3: `MobileScheduleItemCard` 를 `useDraggable` 로 감싸기 + drag handle 시각화
- [ ] T4: 데스크탑 `TableRow` 를 `useDraggable` 로 감싸기 + 드래그 핸들 `⠿` 컬럼 추가
- [ ] T5: `ScheduleTable` 루트에 `DndContext` + sensors + `DragOverlay`
- [ ] T6: `onDragEnd` 핸들러 — `date:` prefix 파싱 후 `onUpdateItem` 호출
- [ ] T7: `npm run build` + 수동 시나리오 검증
